# Plan: <title>

> A plan is an artifact. Write it in one session; hand it to a fresh session to execute
> (this avoids context rot + planning bias bleeding into the build). Keep it scannable.

## Context
Why this work is happening — the problem/need, what prompted it, the intended outcome.
(If you can't write this clearly, you're not ready to plan. Ask first.)

## Scope
- In scope:
- Out of scope (explicitly):

## Approach
The recommended approach only (not every alternative you considered). Name the specific
files/assets/areas to change. Reuse what already exists — reference it by path.

## Steps
1.
2.
3.

## Risks / unknowns
- Anything that might force a re-plan; anything needing research first.

## Verification
How we'll prove this works end-to-end (the verify command/phases, the real run, the
click-through, the reconciliation) — see the active profile's quality gates.

## Done means
The concrete, checkable definition of done for this unit of work.
