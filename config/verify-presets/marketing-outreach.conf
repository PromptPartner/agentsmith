# ── marketing-outreach ────────────────────────────────────────────────────────
# "Shippable" = copy is clean and links work. The actual SEND is gated by HUMAN APPROVAL —
# never add a phase that sends/publishes; verify proves the draft is ready, a human hits send.
# spell :: npx cspell "drafts/**/*.md"
# links :: npx linkinator drafts --silent
