# ── deep-research ──────────────────────────────────────────────────────────────
# "Shippable" = every claim is cited and the cited sources still resolve. No orphan assertions.
# links     :: npx linkinator docs/research --silent   # cited URLs still resolve
# citations :: scripts/check-citations.sh              # every claim traces to a source (write this)
