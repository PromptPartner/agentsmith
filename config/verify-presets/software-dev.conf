# ── software-dev ──────────────────────────────────────────────────────────────
# Swap each command for YOUR stack. "Shippable" = it builds, types check, and tests pass.
# build :: npm run build          # or: go build ./...   | cargo build  | make
# types :: npx tsc --noEmit       # or: mypy .            | (skip if untyped)
# lint  :: npm run lint           # or: golangci-lint run | ruff check .
# test  :: npm test               # or: go test ./...     | pytest -q
# Security: a gate the model can't talk its way past. Uncomment what your stack has.
# deps    :: npm audit --audit-level=high   # or: pip-audit | govulncheck ./... | cargo audit
# secrets :: ./scripts/secret-scan.sh --all # R8, if the harness scripts are installed here
