# ── autonomous-loops ──────────────────────────────────────────────────────────
# "Shippable" = the loop has a checker it can't fake, a capped attempt count, a budget, and a kill switch.
# dryrun :: <your loop cmd> --dry-run        # one full cycle, report-only, no writes
# state  :: scripts/loop-state-check.sh      # state read + pruned every run, no ghost items (write this)
# budget :: scripts/loop-budget-check.sh     # daily cap + degrade rule + kill switch present (write this)
