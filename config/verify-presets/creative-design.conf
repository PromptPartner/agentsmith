# ── creative-design ───────────────────────────────────────────────────────────
# "Shippable" = the asset actually renders/exports and any links resolve. Then eyeball it.
# links  :: npx linkinator ./out --silent      # asset links resolve
# render :: <export/build that proves the asset renders>   # then look at the output, don't assume
