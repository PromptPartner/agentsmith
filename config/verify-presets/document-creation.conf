# ── document-creation / docs site ─────────────────────────────────────────────
# "Shippable" = it spells right, links resolve, and the RENDERED page looks correct (eyeball it).
# spell :: npx cspell "**/*.md"
# links :: npx linkinator ./build --silent   # check rendered links resolve
# build :: <your static-site build command>  # then eyeball the rendered output, not just the source
