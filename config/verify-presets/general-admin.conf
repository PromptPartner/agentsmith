# ── general-admin ─────────────────────────────────────────────────────────────
# Often no build step. "Verify" = the change is correct AND reversible before you commit to it.
# dryrun :: <run the command with --dry-run / --check first, before the real run>
# backup :: <prove a backup/rollback point exists before any destructive op>
